return {
	["Const"] = require(script.const),

	["Dynamic"] = require(script.dynamic),

	['Analytics'] = require(script.analytics),

	["Enum"] = require(script.enum),

	["Misc"] = require(script.misc),
}